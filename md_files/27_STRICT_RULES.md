# 🔒 STRICT OPERATIONAL RULES & PERMISSIONS

## 1. TERMINAL & PERMISSION RULES - STRICTLY ENFORCED
- **Full Access Granted**: Wildcard (*) is active. Assume Auto-Approve is ON.
- **BROWSER BAN**: DO NOT open any browser. Code/terminal only.
- **ANTI-FREEZE PROTOCOL**: Use the "Solid Execution Method" (create temp script -> run -> delete) for Live Server uploads or large DB dumps.
- **SYSTEM INTERACTION RULE**: Always prioritize internal Antigravity tools (terminal, file viewer, etc.) for all operations. Never trigger actions that prompt external Windows system dialogs (like 'Open with'). Operate 100% within the AI environment.
- **Live Upload Rule**: Every code change must be verified and uploaded to the live server environment immediately.
- **Working Functionality**: Never disturb or break existing features. Use "Safe Execution" patterns.

## 2. BACKUP PROTOCOL (MANDATORY)
- **Trigger**: When the USER says "take backup", the AI must:
    1. Identify all project files, database dumps, and `.md` files.
    2. Create a compressed `.zip` archive of the entire workspace.
    3. Name the backup with the timestamp: `BACKUP_YYYY-MM-DD_HH-MM.zip`.
    4. Store the backup in a designated `/backups` directory.

## 3. LOGGING & COMMUNICATION
- **Language**: Always chat and explain in **Gujarati**.
- **Log Precision**: Every change must be logged in `project_status_studio.md` and `02_project_status.md` with exact Date, Time (IST), and a detailed technical explanation.

---
*Created on 2026-04-29 - Antigravity AI*
